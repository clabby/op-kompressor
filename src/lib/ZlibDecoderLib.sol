// SPDX-License-Identifier: MIT
pragma solidity ^0.8.19;

library ZlibDecoderLib {
    /// @notice Decodes a run-length encoded byte string into an uncompressed abi-encoded payload
    /// @param _in The run-length encoded byte string
    /// @return _out The uncompressed abi-encoded payload
    function decode(bytes memory _in) internal pure returns (bytes memory _out) {
        // TODO
    }
}
